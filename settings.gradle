pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "AppRTCMobile"
include ':app'

include ":rtc_base.base_java"
project(":rtc_base.base_java").projectDir = new File(settingsDir, "rtc_base/base_java")

include ':sdk.android.base_java'
project(':sdk.android.base_java').setProjectDir(new File("sdk/android/base_java"))
include ':base_java'
